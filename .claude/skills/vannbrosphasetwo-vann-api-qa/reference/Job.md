<!-- GENERATED by scripts/generate.mjs — do not edit by hand; run scripts/refresh.sh -->
# Job

Integration jobs — list per connection, update.

2 operation(s). Base URL `https://agrierp-vann-api-qa.folio3.site`; every call needs `Authorization: Bearer <token>` (see ../SKILL.md).

## Operations

| Method | Path | operationId |
|---|---|---|
| GET | [`/api/Job`](#op-job-getallbyconnectionid) | Job_GetAllByConnectionID |
| PUT | [`/api/Job/{id}`](#op-job-update) | Job_Update |

<a id="op-job-getallbyconnectionid"></a>
### GET `/api/Job`

operationId `Job_GetAllByConnectionID`

| Param | In | Type | Required | Notes |
|---|---|---|---|---|
| `connectionID` | query | int64 |  | non-nullable (omitted → server default, e.g. 0/false); default `0` |

Request body: _none_

Responses: `200` body not described (spec says `file`)

<a id="op-job-update"></a>
### PUT `/api/Job/{id}`

operationId `Job_Update`

| Param | In | Type | Required | Notes |
|---|---|---|---|---|
| `id` | path | int64 | yes |  |

Request body `jobFM` (application/json), **required**: [JobLastFetchMarkerFormAPIModel](#schema-joblastfetchmarkerformapimodel)

Responses: `200` body not described (spec says `file`)

## Schemas

Types used by these operations, 2 level(s) deep. Links into `_schemas.md` are deeper types; grep that file for `schema-<name>` rather than reading it whole.

<a id="schema-joblastfetchmarkerformapimodel"></a>
### JobLastFetchMarkerFormAPIModel

Required: `id`, `dateTime`, `pageSize`

| Field | Type | Required | Notes |
|---|---|---|---|
| `id` | int64 | yes |  |
| `dateTime` | string (date-time) | yes |  |
| `pageSize` | int32 | yes |  |
