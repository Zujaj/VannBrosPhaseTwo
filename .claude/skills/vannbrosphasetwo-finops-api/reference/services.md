# FinOps services (generated)

<!-- Generated by scripts/extract.mjs from the Postman "FinOps" folder. Do not hand-edit. -->

Source: `AgriERP Product.postman_collection.json`, 24 requests, 21 distinct paths.
Every request is `POST {{AppUrl}}<path>` with `Authorization: Bearer <token>` (see SKILL.md),
except **Auth**, which gets the token from Microsoft Entra ID.

| # | Postman name | Operation | Path |
|---|---|---|---|
| 1 | [Auth](#1) | `token` | `https://login.microsoftonline.com/{{TenantID}}/oauth2/v2.0/token` |
| 2 | [F3AgriCropService](#2) | `get` | `/api/services/F3AgriERPServices/F3AgriCropServices/get` |
| 3 | [F3AgriFarmOperationServices](#3) | `get` | `/api/services/F3AgriERPServices/F3AgriCropServices/get` |
| 4 | [F3AgriCropVarietiesService](#4) | `get` | `/api/services/F3AgriERPServices/F3AgriCropVarietiesServices/get` |
| 5 | [F3AgriProjectService](#5) | `get` | `/api/services/F3AgriERPServices/F3AgriProjectService/get` |
| 6 | [F3AgriMaterialService](#6) | `get` | `/api/services/F3AgriERPServices/F3AgriMaterialServices/get` |
| 7 | [F3AgriSeasonService](#7) | `get` | `/api/services/F3AgriERPServices/F3AgriSeasonServices/get` |
| 8 | [F3AgriFarmOperationService](#8) | `get` | `/api/services/F3AgriERPServices/F3AgriFarmOperationServices/get` |
| 9 | [F3AgriCropMaterialGroupService](#9) | `get` | `/api/services/F3AgriERPServices/F3AgriCropMaterialGroupServices/get` |
| 10 | [F3AgriCustomerService](#10) | `get` | `/api/services/F3AgriERPServices/F3AgriCustomerServices/get` |
| 11 | [F3AgriUOMService](#11) | `get` | `/api/services/F3AgriERPServices/F3AgriUOMServices/get` |
| 12 | [F3AgriResourceGroupService](#12) | `get` | `/api/services/F3AgriERPServices/F3AgriResourceGroupServices/get` |
| 13 | [F3AgriResourceService](#13) | `get` | `/api/services/F3AgriERPServices/F3AgriResourceServices/get` |
| 14 | [F3AgriIrrigationMethodService](#14) | `create` | `/api/services/F3AgriERPServices/F3AgriIrrigationMethodServices/create` |
| 15 | [F3AgriPlotFieldServices](#15) | `create` | `/api/services/F3AgriERPServices/F3AgriPlotFieldServices/create` |
| 16 | [F3AgriProjectService createProject](#16) | `createProject` | `/api/services/F3AgriSalesServiceGroup/F3AgriSalesProjectService/createProject` |
| 17 | [F3AgriProjectService CreateWBSLines](#17) | `create` | `/api/services/F3AgriERPServices/F3AgriPlotFieldServices/create` |
| 18 | [F3AgriIrrigationSourceServices](#18) | `create` | `/api/services/F3AgriERPServices/F3AgriIrrigationSourceServices/create` |
| 19 | [F3AgriWorkOrderServices](#19) | `createOrUpdateWO` | `/api/services/F3AgriERPServices/F3AgriWorkOrderServices/createOrUpdateWO` |
| 20 | [F3AgriWMSLocationServices](#20) | `get` | `/api/services/F3AgriERPServices/F3AgriWMSLocationServices/get` |
| 21 | [createProjectItemJournal](#21) | `createProjectItemJournal` | `/api/services/F3AgriERPServices/F3AgriJournalService/createProjectItemJournal` |
| 22 | [F3AgriINVItemTransferService itemReturn](#22) | `itemReturn` | `/api/services/F3AgriInventoryServiceGroup/F3AgriINVItemTransferService/itemReturn` |
| 23 | [F3AgriINVItemTransferService itemReturn Copy](#23) | `itemReturn` | `/api/services/F3AgriInventoryServiceGroup/F3AgriINVItemTransferService/itemReturn` |
| 24 | [F3AgriUnitOfConversion](#24) | `get` | `/api/services/F3AgriERPServices/F3AgriUnitOfConversion/get` |

## Paths used by more than one request

- `/api/services/F3AgriERPServices/F3AgriCropServices/get`: "F3AgriCropService", "F3AgriFarmOperationServices"
- `/api/services/F3AgriERPServices/F3AgriPlotFieldServices/create`: "F3AgriPlotFieldServices", "F3AgriProjectService CreateWBSLines"
- `/api/services/F3AgriInventoryServiceGroup/F3AgriINVItemTransferService/itemReturn`: "F3AgriINVItemTransferService itemReturn", "F3AgriINVItemTransferService itemReturn Copy"

<a id="1"></a>
## 1. Auth

`POST https://login.microsoftonline.com/{{TenantID}}/oauth2/v2.0/token`

Form body (`application/x-www-form-urlencoded`):

| Key | Value |
|---|---|
| `client_id` | `{{ClientId}}` |
| `client_secret` | `{{AppKey}}` |
| `scope` | `{{AppUrl}}/.default` |
| `grant_type` | `client_credentials` |

Post-response script saves `access_token` into the `agrierp_fno_access_token` environment variable.

<a id="2"></a>
## 2. F3AgriCropService

`POST {{AppUrl}}/api/services/F3AgriERPServices/F3AgriCropServices/get`

Body (`application/json`, as saved in Postman — inline `//` comments are Postman-only and must be removed before sending):

```jsonc
{
    "doc": {  
        "ID": 0,      
        "DateTime": "2000-10-08T10:34:06Z",
        "PageSize": 100,
        "PageNo": 1,
        "LegalEntity":"vbs"
    }
}
```

<a id="3"></a>
## 3. F3AgriFarmOperationServices

`POST {{AppUrl}}/api/services/F3AgriERPServices/F3AgriCropServices/get`

Body (`application/json`, as saved in Postman — inline `//` comments are Postman-only and must be removed before sending):

```jsonc
{
    "doc": {  
        "ID": 0,      
        "DateTime": "2000-10-08T10:34:06Z",
        "PageSize": 100,
        "PageNo": 1,
        "LegalEntity":"vbs"
    }
}
```

<a id="4"></a>
## 4. F3AgriCropVarietiesService

`POST {{AppUrl}}/api/services/F3AgriERPServices/F3AgriCropVarietiesServices/get`

Body (`application/json`, as saved in Postman — inline `//` comments are Postman-only and must be removed before sending):

```jsonc
{
    "doc": {  
        "ID": 0,      
        "DateTime": "2025-10-08T10:34:06Z",
        "PageSize": 100,
        "PageNo": 1,
        "LegalEntity":"vbs"
    }
}
```

<a id="5"></a>
## 5. F3AgriProjectService

`POST {{AppUrl}}/api/services/F3AgriERPServices/F3AgriProjectService/get`

Body (`application/json`, as saved in Postman — inline `//` comments are Postman-only and must be removed before sending):

```jsonc
{
    "doc": {  
        "ID": 0,      
        "DateTime": "2025-10-08T10:34:06Z",
        "PageSize": 100,
        "PageNo": 1,
        "LegalEntity":"vbs"
    }
}
```

<a id="6"></a>
## 6. F3AgriMaterialService

`POST {{AppUrl}}/api/services/F3AgriERPServices/F3AgriMaterialServices/get`

Body (`application/json`, as saved in Postman — inline `//` comments are Postman-only and must be removed before sending):

```jsonc
{
    "doc": {  
        "ID": 0,      
        "DateTime": "2000-10-08T10:34:06Z",
        "PageSize": 1000,
        "PageNo": 1,
        "LegalEntity":"vbs"
    }
}
```

<a id="7"></a>
## 7. F3AgriSeasonService

`POST {{AppUrl}}/api/services/F3AgriERPServices/F3AgriSeasonServices/get`

Body (`application/json`, as saved in Postman — inline `//` comments are Postman-only and must be removed before sending):

```jsonc
{
    "doc": {  
        "ID": 0,      
        "DateTime": "2025-10-08T10:34:06Z",
        "PageSize": 100,
        "PageNo": 1,
        "LegalEntity":"vbs"
    }
}
```

<a id="8"></a>
## 8. F3AgriFarmOperationService

`POST {{AppUrl}}/api/services/F3AgriERPServices/F3AgriFarmOperationServices/get`

Body (`application/json`, as saved in Postman — inline `//` comments are Postman-only and must be removed before sending):

```jsonc
{
    "doc": {  
        "ID": 0,      
        "DateTime": "2020-10-08T10:34:06Z",
        "PageSize": 100,
        "PageNo": 1,
        "LegalEntity":"vbs"
    }
}
```

<a id="9"></a>
## 9. F3AgriCropMaterialGroupService

`POST {{AppUrl}}/api/services/F3AgriERPServices/F3AgriCropMaterialGroupServices/get`

Body (`application/json`, as saved in Postman — inline `//` comments are Postman-only and must be removed before sending):

```jsonc
{
    "doc": {  
        "ID": 0,      
        "DateTime": "2020-10-08T10:34:06Z",
        "PageSize": 100,
        "PageNo": 1,
        "LegalEntity":"vbs"
    }
}
```

<a id="10"></a>
## 10. F3AgriCustomerService

`POST {{AppUrl}}/api/services/F3AgriERPServices/F3AgriCustomerServices/get`

Body (`application/json`, as saved in Postman — inline `//` comments are Postman-only and must be removed before sending):

```jsonc
{
    "doc": {  
        "ID": 0,      
        "DateTime": "2020-10-08T10:34:06Z",
        "PageSize": 100,
        "PageNo": 1,
        "LegalEntity":"vbs"
    }
}
```

<a id="11"></a>
## 11. F3AgriUOMService

`POST {{AppUrl}}/api/services/F3AgriERPServices/F3AgriUOMServices/get`

Body (`application/json`, as saved in Postman — inline `//` comments are Postman-only and must be removed before sending):

```jsonc
{
    "doc": {  
        "ID": 0,      
        "DateTime": "2020-10-08T10:34:06Z",
        "PageSize": 10000,
        "PageNo": 1,
        "LegalEntity":"vbs"
    }
}
```

<a id="12"></a>
## 12. F3AgriResourceGroupService

`POST {{AppUrl}}/api/services/F3AgriERPServices/F3AgriResourceGroupServices/get`

Body (`application/json`, as saved in Postman — inline `//` comments are Postman-only and must be removed before sending):

```jsonc
{
    "doc": {  
        "ID": 0,      
        "DateTime": "2020-10-08T10:34:06Z",
        "PageSize": 10000,
        "PageNo": 1,
        "LegalEntity":"vbs"
    }
}
```

<a id="13"></a>
## 13. F3AgriResourceService

`POST {{AppUrl}}/api/services/F3AgriERPServices/F3AgriResourceServices/get`

Body (`application/json`, as saved in Postman — inline `//` comments are Postman-only and must be removed before sending):

```jsonc
{
    "doc": {  
        "ID": 0,      
        "DateTime": "2020-10-08T10:34:06Z",
        "PageSize": 10000,
        "PageNo": 1,
        "LegalEntity":"vbs"
    }
}
```

<a id="14"></a>
## 14. F3AgriIrrigationMethodService

`POST {{AppUrl}}/api/services/F3AgriERPServices/F3AgriIrrigationMethodServices/create`

Body (`application/json`, as saved in Postman — inline `//` comments are Postman-only and must be removed before sending):

```jsonc
{
    "doc": {  
        "IrrigationMethodId":"Irrigation Method 35",
        "Description":"Testing API",
        "LegalEntity":"vbs"
    }
}
```

<a id="15"></a>
## 15. F3AgriPlotFieldServices

`POST {{AppUrl}}/api/services/F3AgriERPServices/F3AgriPlotFieldServices/create`

Body (`application/json`, as saved in Postman — inline `//` comments are Postman-only and must be removed before sending):

```jsonc
{
  "doc": {
    "RecId": 5637144576, // Field.ID cross ref RecId
    "PlotField": "PlotFieldTesting45", // Field.Code
    "Description": "Testing API description", // Field.Name
    "HarvestWarehouse": "150",
    "ConsumptionWarehouse": "150",
    "HierarchyNode": "Testing API HierarchyNode", // string.Empty
    "IrrigationMethod": "irrigation2", // Field.Irrigation.Code
    "IrrigationSources": "source4;source2", // Field.Soruces.Codes (Join with delimiter)
    "TotalArea": 1000, // Field.Area
    "TotalOperationArea": 500, // Field.Area
    "LegalEntity": "vbs" // default
  }
}
```

<a id="16"></a>
## 16. F3AgriProjectService createProject

`POST {{AppUrl}}/api/services/F3AgriSalesServiceGroup/F3AgriSalesProjectService/createProject`

Body (`application/json`, as saved in Postman — inline `//` comments are Postman-only and must be removed before sending):

```jsonc
{
    "doc": {
        "ProjId": "PRJ_000117",
        "ProjectName": "2026.PF-2",
        "ProjGroupId": "03",
        "CustAccount": "AIR-000001",
        "ProjType": 4,
        "LegalEntity": "vbs",
        "Plot": "PF-2",
        "Crop": "00344",
        "CropYear": 2025,
        "PlannedOperationalArea": 2,
        "ProjectStartDate": "2026-05-29"
    }
}
```

<a id="17"></a>
## 17. F3AgriProjectService CreateWBSLines

`POST {{AppUrl}}/api/services/F3AgriERPServices/F3AgriPlotFieldServices/create`

Body (`application/json`, as saved in Postman — inline `//` comments are Postman-only and must be removed before sending):

```jsonc
{
  "doc": {
    "RecId": 5637144576, // Field.ID cross ref RecId
    "PlotField": "PlotFieldTesting45", // Field.Code
    "Description": "Testing API description", // Field.Name
    "HarvestWarehouse": "150",
    "ConsumptionWarehouse": "150",
    "HierarchyNode": "Testing API HierarchyNode", // string.Empty
    "IrrigationMethod": "irrigation2", // Field.Irrigation.Code
    "IrrigationSources": "source4;source2", // Field.Soruces.Codes (Join with delimiter)
    "TotalArea": 1000, // Field.Area
    "TotalOperationArea": 500, // Field.Area
    "LegalEntity": "vbs" // default
  }
}
```

<a id="18"></a>
## 18. F3AgriIrrigationSourceServices

`POST {{AppUrl}}/api/services/F3AgriERPServices/F3AgriIrrigationSourceServices/create`

Body (`application/json`, as saved in Postman — inline `//` comments are Postman-only and must be removed before sending):

```jsonc
{
  "doc": {
    "SourceId": "Source34",
    "Description": "Testing API",
    "LegalEntity": "vbs"
  }
}
```

<a id="19"></a>
## 19. F3AgriWorkOrderServices

`POST {{AppUrl}}/api/services/F3AgriERPServices/F3AgriWorkOrderServices/createOrUpdateWO`

Body (`application/json`, as saved in Postman — inline `//` comments are Postman-only and must be removed before sending):

```jsonc
{
    "doc": {
        "ProjectId": "PRJ_000022",
        "OperationId": "2001",
        "TaskId": "1240",
        "Activity": "",
        "WOSequence": "WO-01",
        "ActivityType": "adhoc",
        "LegalEntity": "vbs"
    }
}
```

<a id="20"></a>
## 20. F3AgriWMSLocationServices

`POST {{AppUrl}}/api/services/F3AgriERPServices/F3AgriWMSLocationServices/get`

Body (`application/json`, as saved in Postman — inline `//` comments are Postman-only and must be removed before sending):

```jsonc
{
    "doc": {  
        "ID": 0,      
        "DateTime": "2022-10-08T10:34:06Z",
        "PageSize": 100,
        "PageNo": 1,
        "LegalEntity":"vbs"
    }
}
```

<a id="21"></a>
## 21. createProjectItemJournal

`POST {{AppUrl}}/api/services/F3AgriERPServices/F3AgriJournalService/createProjectItemJournal`

Body (`application/json`, as saved in Postman — inline `//` comments are Postman-only and must be removed before sending):

```jsonc
{
    "doc": {
        "MaterialLines": [
          
            {
                "ProjectDate": "2025-11-13T11:58:56.875",
                "ItemId": "00166",
                "Quantity": 50.0,
                "Unit": "oz",
                "Warehouse":"152",
                "Location":"",
                "BatchNumber":"BAT-001"
            }
        ],
        "ProjectId": "PRJ_000022",
        "Activity": "VBS-000059",
        "OperationId": "1240",
        "WOSequence": "WO-01",
        "LegalEntity": "vbs"
    }
}
```

<a id="22"></a>
## 22. F3AgriINVItemTransferService itemReturn

`POST {{AppUrl}}/api/services/F3AgriInventoryServiceGroup/F3AgriINVItemTransferService/itemReturn`

Body (`application/json`, as saved in Postman — inline `//` comments are Postman-only and must be removed before sending):

```jsonc
{
  "request": {
    "TransferJournalLines": [
      {
        "ItemId": "00004",
        "Quantity": 10,
        "ToWarehouse": "0101",
        "ToLocation": "CHMCLSHED",
        "ToBatchNumber": "Opening"
      }
    ],
    "Company": "vbs",
    "languageId": "en-us",
    "TransferJournalHeader": {}
  }
}
```

_35 commented-out lines (earlier variants of this body) omitted; see collection.finops.json._

<a id="23"></a>
## 23. F3AgriINVItemTransferService itemReturn Copy

`POST {{AppUrl}}/api/services/F3AgriInventoryServiceGroup/F3AgriINVItemTransferService/itemReturn`

Body (`application/json`, as saved in Postman — inline `//` comments are Postman-only and must be removed before sending):

```jsonc
{
  "request": {
    "TransferJournalLines": [
      {
        "ItemId": "00017",
        "Quantity": 12.8,
        "ToWarehouse": "0101",
        "ToLocation": "CHMCLSHED",
        "ToBatchNumber": "Opening"
      }
    ],
    "Company": "vbs",
    "languageId": "en-us",
    "TransferJournalHeader": {}
  }
}
```

_51 commented-out lines (earlier variants of this body) omitted; see collection.finops.json._

<a id="24"></a>
## 24. F3AgriUnitOfConversion

`POST {{AppUrl}}/api/services/F3AgriERPServices/F3AgriUnitOfConversion/get`

Body (`application/json`, as saved in Postman — inline `//` comments are Postman-only and must be removed before sending):

```jsonc
{
    "doc": {  
        "ID": 0,      
        "DateTime": "2000-10-08T10:34:06Z",
        "PageSize": 1000,
        "PageNo": 1,
        "LegalEntity":"vbs"
    }
}
```
