{
  "mcpServers": {
    "playwright-test": {
      "command": "playwright/node_modules/.bin/playwright",
      "args": [
        "run-test-mcp-server",
        "--config",
        "playwright"
      ]
    }
  }
}
